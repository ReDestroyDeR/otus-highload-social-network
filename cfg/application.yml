logger_config:
  level: "INFO"

pg_config:
  host: "localhost"
  port: 5432
  database: "postgres"
  user: "postgres"
  password: "postgres"

auth_config:
  session_lifetime_seconds: 60
  invalid_sessions_cache_limit: 100
